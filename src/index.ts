import { Options } from "prettier";

const config: Options = {
  printWidth: 100,
  arrowParens: "avoid",
  singleQuote: false,
  trailingComma: "none",
  bracketSameLine: true
};

export default config;
